from ..qt import *

class SpikeGlxWidget(QWidget):
  def __init__(self, config, stub):
    super().__init__()
    #self.config = config
    #self.stub = stub
#
    #layout = QVBoxLayout()
    #refresh = QPushButton('Refresh')
    #layout.addWidget(refresh)
#
    #def on_refresh():
    #  
#
    #refresh.clicked.connect(on_fresh)
